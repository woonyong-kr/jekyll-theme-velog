<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="ko-KR"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://woonyong-kr.github.io/jekyll-theme-velog/feed.xml" rel="self" type="application/atom+xml" /><link href="https://woonyong-kr.github.io/jekyll-theme-velog/" rel="alternate" type="text/html" hreflang="ko-KR" /><updated>2026-05-08T14:36:07+09:00</updated><id>https://woonyong-kr.github.io/jekyll-theme-velog/feed.xml</id><title type="html">Velog Style Jekyll Theme</title><subtitle>velog 감성의 개발 블로그를 GitHub Pages와 Jekyll로 구성한 오픈소스 starter theme</subtitle><entry><title type="html">Velog Style Jekyll Theme 소개</title><link href="https://woonyong-kr.github.io/jekyll-theme-velog/introducing-velog-style-jekyll-theme/" rel="alternate" type="text/html" title="Velog Style Jekyll Theme 소개" /><published>2026-04-02T09:00:00+09:00</published><updated>2026-04-02T09:00:00+09:00</updated><id>https://woonyong-kr.github.io/jekyll-theme-velog/introducing-velog-style-jekyll-theme</id><content type="html" xml:base="https://woonyong-kr.github.io/jekyll-theme-velog/introducing-velog-style-jekyll-theme/"><![CDATA[<p>이 테마는 <code class="language-plaintext highlighter-rouge">읽기 편한 글 목록</code>, <code class="language-plaintext highlighter-rouge">시리즈</code>, <code class="language-plaintext highlighter-rouge">태그</code>, <code class="language-plaintext highlighter-rouge">검색</code>, <code class="language-plaintext highlighter-rouge">GitHub 프로필 동기화</code>, <code class="language-plaintext highlighter-rouge">기여 그래프</code>를 한 번에 갖춘 Jekyll 기반 개발 블로그 출발점입니다.</p>

<p>단순히 보기 좋은 화면만 넣는 대신, 저장소를 복제한 뒤 어떻게 설정하고 운영하는지까지 문서형 데모로 함께 제공하는 것을 목표로 만들었습니다. 지금 보고 있는 이 블로그 자체가 그 데모입니다.</p>

<h2 id="이-테마로-뭘-만들-수-있나요">이 테마로 뭘 만들 수 있나요?</h2>

<p>GitHub Pages를 이용한 <strong>무료 개발 블로그</strong>를 만들 수 있습니다. 별도 서버나 비용 없이, 이 저장소를 fork하고 설정 파일 몇 줄만 바꾸면 됩니다.</p>

<p>완성된 블로그에는 아래 기능이 모두 포함되어 있습니다.</p>

<ul>
  <li><strong>글 목록</strong>: 태그 필터, 검색, 페이지 없이 스크롤로 더 불러오기</li>
  <li><strong>시리즈</strong>: 연관된 글을 묶어서 카드형으로 탐색</li>
  <li><strong>GitHub 프로필</strong>: 이름, 아바타, 기여 그래프 자동 동기화</li>
  <li><strong>다크 모드</strong>: 시스템 설정 자동 감지 + 토글</li>
  <li><strong>댓글</strong>: Giscus(GitHub Discussions 기반) 또는 Disqus 선택</li>
  <li><strong>검색</strong>: 기본 로컬 검색 또는 Algolia 고급 검색</li>
</ul>

<h2 id="화면-미리보기">화면 미리보기</h2>

<h3 id="홈">홈</h3>

<p><img src="/jekyll-theme-velog/assets/images/docs/home-preview.png" alt="홈 화면 미리보기" /></p>

<p>홈에서는 프로필, 기여 그래프, 태그 목록, 글 목록을 한 화면 흐름 안에서 볼 수 있습니다.</p>

<h3 id="시리즈">시리즈</h3>

<p><img src="/jekyll-theme-velog/assets/images/docs/series-preview.png" alt="시리즈 화면 미리보기" /></p>

<p>시리즈 탭은 카드형 레이아웃으로, 연재 묶음을 한눈에 탐색할 수 있습니다.</p>

<h3 id="글-상세">글 상세</h3>

<p><img src="/jekyll-theme-velog/assets/images/docs/post-preview.png" alt="글 상세 화면 미리보기" /></p>

<p>시리즈명, 제목, 태그, 본문, 이전/다음 포스트까지 한 흐름으로 이어집니다.</p>

<h2 id="github-pages가-처음이라면">GitHub Pages가 처음이라면</h2>

<blockquote>
  <p><strong>GitHub Pages</strong>는 GitHub 저장소를 그대로 웹사이트로 호스팅해주는 무료 서비스입니다.<br />
코드를 push하면 자동으로 빌드되어 <code class="language-plaintext highlighter-rouge">username.github.io</code> 주소로 블로그가 열립니다.</p>
</blockquote>

<p>필요한 것은 GitHub 계정뿐입니다. 코딩 경험이 없어도 이 저장소를 fork하고 설정 파일을 웹에서 직접 편집하는 것만으로 블로그를 만들 수 있습니다.</p>

<h2 id="5분-안에-배포하는-방법">5분 안에 배포하는 방법</h2>

<h3 id="1-이-저장소-fork하기">1. 이 저장소 fork하기</h3>

<p>이 페이지 오른쪽 위 <strong>Fork</strong> 버튼을 클릭합니다.<br />
저장소 이름을 정할 때 아래 표를 참고하세요.</p>

<table>
  <thead>
    <tr>
      <th>원하는 주소</th>
      <th>저장소 이름</th>
      <th><code class="language-plaintext highlighter-rouge">_config.yml</code>의 <code class="language-plaintext highlighter-rouge">baseurl</code></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">username.github.io</code></td>
      <td><code class="language-plaintext highlighter-rouge">username.github.io</code></td>
      <td><code class="language-plaintext highlighter-rouge">""</code> (빈값)</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">username.github.io/my-blog</code></td>
      <td><code class="language-plaintext highlighter-rouge">my-blog</code></td>
      <td><code class="language-plaintext highlighter-rouge">"/my-blog"</code></td>
    </tr>
  </tbody>
</table>

<p><code class="language-plaintext highlighter-rouge">username</code>은 본인 GitHub 아이디입니다.</p>

<h3 id="2-_configyml-수정하기">2. <code class="language-plaintext highlighter-rouge">_config.yml</code> 수정하기</h3>

<p>fork한 저장소에서 <code class="language-plaintext highlighter-rouge">_config.yml</code> 파일을 열고 연필 아이콘(✏️)으로 편집합니다.</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">title</span><span class="pi">:</span> <span class="s">내 블로그 이름</span>
<span class="na">description</span><span class="pi">:</span> <span class="s">블로그 한 줄 설명</span>
<span class="na">url</span><span class="pi">:</span> <span class="s2">"</span><span class="s">https://username.github.io"</span>   <span class="c1"># 본인 GitHub 아이디로 변경</span>
<span class="na">baseurl</span><span class="pi">:</span> <span class="s2">"</span><span class="s">/my-blog"</span>                 <span class="c1"># 저장소 이름에 맞게 변경</span>
</code></pre></div></div>

<p>수정 후 <strong>Commit changes</strong>를 누르면 자동 빌드가 시작됩니다.</p>

<h3 id="3-github-pages-활성화하기">3. GitHub Pages 활성화하기</h3>

<p>저장소 <strong>Settings → Pages</strong>로 이동해서:</p>

<ol>
  <li>Source: <code class="language-plaintext highlighter-rouge">Deploy from a branch</code></li>
  <li>Branch: <code class="language-plaintext highlighter-rouge">gh-pages</code>, Folder: <code class="language-plaintext highlighter-rouge">/ (root)</code></li>
  <li>Save</li>
</ol>

<p>빌드가 완료되면 설정한 주소로 블로그에 접속할 수 있습니다. 첫 빌드는 1~2분 정도 걸립니다.</p>

<h2 id="디렉터리-구조-한눈에-보기">디렉터리 구조 한눈에 보기</h2>

<p>처음에는 이 다섯 묶음만 알면 됩니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>_config.yml               사이트 URL, 제목, 외부 연동 설정
_data/profile.yml         기본 프로필 (GitHub 미연동 시 표시)
_data/theme.yml           UI 옵션, 탭 노출, 검색 문구 등
_data/series.yml          시리즈 제목과 설명
_posts/                   블로그 글 파일들 (날짜-제목.md 형식)
</code></pre></div></div>

<p>CSS와 레이아웃은 이미 완성되어 있습니다. 처음에는 HTML/CSS를 열지 않아도 됩니다.</p>

<h2 id="이-테마가-특히-잘-맞는-경우">이 테마가 특히 잘 맞는 경우</h2>

<ul>
  <li>GitHub Pages 위에 개발 블로그를 빠르게 올리고 싶은 경우</li>
  <li>velog처럼 가볍게 읽히는 목록 구조를 선호하는 경우</li>
  <li>글, 시리즈, 태그, 검색이 처음부터 갖춰진 출발점이 필요한 경우</li>
  <li>GitHub 프로필을 블로그 홈에 보여주고 싶은 경우</li>
</ul>

<h2 id="데모-글이-하는-역할">데모 글이 하는 역할</h2>

<p>지금 보고 있는 이 블로그의 <code class="language-plaintext highlighter-rouge">_posts</code> 폴더에는 lorem ipsum 대신 <strong>테마 사용 설명서 역할을 하는 글들</strong>이 들어 있습니다.</p>

<ul>
  <li>로컬 설치 방법</li>
  <li>설정 파일 커스터마이징</li>
  <li>글과 시리즈 작성 방법</li>
  <li>GitHub Pages 배포</li>
  <li>GitHub 프로필 동기화</li>
</ul>

<p>fork 후 이 글들을 지우고 본인 글로 채워도 되고, 참고 자료로 유지해도 됩니다.</p>

<h2 id="다음-단계">다음 단계</h2>

<p>이 시리즈를 순서대로 읽으면 처음부터 배포까지 전체 흐름을 파악할 수 있습니다.</p>

<ol>
  <li><strong><a href="/jekyll-theme-velog/installing-the-theme-locally/">로컬에서 테마를 설치하고 미리보기하는 가장 빠른 방법</a></strong> — Ruby 설치부터 로컬 미리보기까지</li>
  <li><strong><a href="/jekyll-theme-velog/customizing-profile-navigation-and-home/">프로필, 탭, 홈 화면을 테마 설정으로 커스터마이징하기</a></strong> — 설정 파일로 내 블로그로 만들기</li>
  <li><strong><a href="/jekyll-theme-velog/writing-posts-series-and-thumbnails/">글, 시리즈, 태그, 썸네일을 이 테마에서 다루는 방법</a></strong> — 첫 번째 포스트 작성하기</li>
  <li><strong><a href="/jekyll-theme-velog/deploying-with-github-pages-and-jenkins/">GitHub Pages와 Jenkins로 테마를 배포하는 방법</a></strong> — 실제 배포하기</li>
  <li><strong><a href="/jekyll-theme-velog/github-profile-sync-and-contribution-graph/">GitHub 프로필 동기화와 기여 그래프를 붙이는 방법</a></strong> — GitHub 프로필 연결하기</li>
</ol>]]></content><author><name></name></author><category term="Jekyll Theme" /><category term="Velog Style" /><category term="GitHub Pages" /><category term="Open Source" /><summary type="html"><![CDATA[velog의 읽기 흐름을 참고해 Jekyll과 GitHub Pages에 맞게 다시 구성한 오픈소스 개발 블로그 테마를 소개합니다.]]></summary></entry><entry><title type="html">로컬에서 테마를 설치하고 미리보기하는 가장 빠른 방법</title><link href="https://woonyong-kr.github.io/jekyll-theme-velog/installing-the-theme-locally/" rel="alternate" type="text/html" title="로컬에서 테마를 설치하고 미리보기하는 가장 빠른 방법" /><published>2026-04-01T09:00:00+09:00</published><updated>2026-04-01T09:00:00+09:00</updated><id>https://woonyong-kr.github.io/jekyll-theme-velog/installing-the-theme-locally</id><content type="html" xml:base="https://woonyong-kr.github.io/jekyll-theme-velog/installing-the-theme-locally/"><![CDATA[<blockquote>
  <p>글을 GitHub 웹에서 직접 작성할 계획이라면 이 글은 건너뛰어도 됩니다.<br />
테마를 수정하거나 배포 전 미리보기가 필요한 경우에만 로컬 실행이 필요합니다.</p>
</blockquote>

<h2 id="준비물">준비물</h2>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>필수 여부</th>
      <th>설명</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Ruby 3.2.x</td>
      <td><strong>필수</strong></td>
      <td>Jekyll은 Ruby 기반입니다</td>
    </tr>
    <tr>
      <td>Bundler</td>
      <td><strong>필수</strong></td>
      <td>gem 의존성 관리 도구</td>
    </tr>
    <tr>
      <td>Git</td>
      <td><strong>필수</strong></td>
      <td>저장소 복제용</td>
    </tr>
    <tr>
      <td>GitHub 토큰</td>
      <td>선택</td>
      <td>프로필/기여 그래프 동기화 시 필요</td>
    </tr>
  </tbody>
</table>

<p>GitHub 연동 없이도 블로그는 정상 작동합니다. 프로필과 잔디 그래프만 샘플 상태를 유지합니다.</p>

<h2 id="ruby-설치하기">Ruby 설치하기</h2>

<p>이 저장소는 <strong>Ruby 3.2.9</strong> 기준으로 검증했습니다. <code class="language-plaintext highlighter-rouge">.ruby-version</code> 파일이 포함되어 있어서 버전 관리자를 쓰면 자동으로 맞춰집니다.</p>

<h3 id="macos">macOS</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># rbenv 설치 (Homebrew 필요)</span>
brew <span class="nb">install </span>rbenv ruby-build

<span class="c"># 쉘에 rbenv 초기화 추가</span>
<span class="nb">echo</span> <span class="s1">'eval "$(rbenv init -)"'</span> <span class="o">&gt;&gt;</span> ~/.zshrc
<span class="nb">source</span> ~/.zshrc

<span class="c"># Ruby 3.2.9 설치</span>
rbenv <span class="nb">install </span>3.2.9
rbenv global 3.2.9

<span class="c"># 버전 확인</span>
ruby <span class="nt">-v</span>
</code></pre></div></div>

<h3 id="windows">Windows</h3>

<p><a href="https://rubyinstaller.org/">RubyInstaller</a>에서 <strong>Ruby+Devkit 3.2.x</strong> 버전을 받아 설치합니다.<br />
설치 마지막 단계에서 <code class="language-plaintext highlighter-rouge">MSYS2 development toolchain</code> 체크박스를 반드시 선택하세요.</p>

<h3 id="linux-ubuntudebian">Linux (Ubuntu/Debian)</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get <span class="nb">install </span>rbenv
rbenv <span class="nb">install </span>3.2.9
rbenv global 3.2.9
</code></pre></div></div>

<h2 id="저장소-복제-및-의존성-설치">저장소 복제 및 의존성 설치</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 저장소 복제 (본인 fork 주소로 변경)</span>
git clone https://github.com/username/my-blog.git
<span class="nb">cd </span>my-blog

<span class="c"># gem 설치</span>
<span class="nv">BUNDLE_FORCE_RUBY_PLATFORM</span><span class="o">=</span><span class="nb">true </span>bundle <span class="nb">install</span>
</code></pre></div></div>

<blockquote>
  <p><strong><code class="language-plaintext highlighter-rouge">BUNDLE_FORCE_RUBY_PLATFORM=true</code>가 왜 필요한가요?</strong><br />
Apple Silicon(M1/M2/M3) Mac 또는 일부 환경에서 네이티브 gem 설치 오류가 발생할 수 있습니다.<br />
이 옵션을 붙이면 Ruby 플랫폼용 gem을 강제로 사용해서 대부분의 오류를 피할 수 있습니다.</p>
</blockquote>

<h2 id="로컬-서버-실행">로컬 서버 실행</h2>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">BUNDLE_FORCE_RUBY_PLATFORM</span><span class="o">=</span><span class="nb">true </span>bundle <span class="nb">exec </span>jekyll serve
</code></pre></div></div>

<p>브라우저에서 <code class="language-plaintext highlighter-rouge">http://127.0.0.1:4000/</code> 으로 접속하면 미리보기가 열립니다.</p>

<p><code class="language-plaintext highlighter-rouge">baseurl</code>을 설정했다면 주소가 달라집니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>baseurl: ""           → http://127.0.0.1:4000/
baseurl: "/my-blog"   → http://127.0.0.1:4000/my-blog/
</code></pre></div></div>

<h2 id="github-프로필과-기여-그래프-연동하기-선택">GitHub 프로필과 기여 그래프 연동하기 (선택)</h2>

<p>홈 화면에 본인 GitHub 프로필과 잔디 그래프를 표시하려면 아래 단계가 필요합니다.</p>

<h3 id="1-github-토큰-준비">1. GitHub 토큰 준비</h3>

<p>아래 세 가지 방법 중 하나를 선택합니다.</p>

<p><strong>방법 A — GitHub CLI 로그인 (가장 간단)</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># GitHub CLI 설치 후</span>
gh auth login
</code></pre></div></div>

<p><strong>방법 B — 환경 변수로 토큰 설정</strong></p>

<p>GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens에서 토큰을 생성합니다. 필요한 권한은 <code class="language-plaintext highlighter-rouge">read:user</code> 하나면 충분합니다.</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">GITHUB_TOKEN</span><span class="o">=</span>ghp_xxxxxxxxxxxxxxxxxxxx
</code></pre></div></div>

<h3 id="2-동기화-스크립트-실행">2. 동기화 스크립트 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ruby scripts/fetch_github_contributions.rb
</code></pre></div></div>

<p>성공하면 아래 두 파일이 생성됩니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>_data/github_profile_cache.json      프로필 정보
_data/github_contributions_cache.json  기여 그래프 데이터
</code></pre></div></div>

<h3 id="3-_datathemeyml-설정-확인">3. <code class="language-plaintext highlighter-rouge">_data/theme.yml</code> 설정 확인</h3>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">hero</span><span class="pi">:</span>
  <span class="na">github_contributions</span><span class="pi">:</span>
    <span class="na">enabled</span><span class="pi">:</span> <span class="no">true</span>
    <span class="na">username</span><span class="pi">:</span> <span class="s">your-github-id</span>   <span class="c1"># 본인 GitHub 아이디로 변경</span>

<span class="na">profile</span><span class="pi">:</span>
  <span class="na">github_sync</span><span class="pi">:</span>
    <span class="na">enabled</span><span class="pi">:</span> <span class="no">true</span>
</code></pre></div></div>

<p>이후 <code class="language-plaintext highlighter-rouge">jekyll serve</code>를 재실행하면 홈에 GitHub 프로필이 표시됩니다.</p>

<h2 id="자주-발생하는-오류와-해결-방법">자주 발생하는 오류와 해결 방법</h2>

<h3 id="bundle-install-실패"><code class="language-plaintext highlighter-rouge">bundle install</code> 실패</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>오류: An error occurred while installing ffi
</code></pre></div></div>

<p>해결:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">BUNDLE_FORCE_RUBY_PLATFORM</span><span class="o">=</span><span class="nb">true </span>bundle <span class="nb">install</span>
</code></pre></div></div>

<p>또는 Ruby 버전이 3.2.x인지 <code class="language-plaintext highlighter-rouge">ruby -v</code>로 먼저 확인하세요.</p>

<hr />

<h3 id="스타일이-깨지거나-이미지가-안-보일-때">스타일이 깨지거나 이미지가 안 보일 때</h3>

<p><code class="language-plaintext highlighter-rouge">_config.yml</code>의 <code class="language-plaintext highlighter-rouge">baseurl</code>이 저장소 이름과 다를 가능성이 높습니다.</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 저장소 이름이 my-blog라면</span>
<span class="na">baseurl</span><span class="pi">:</span> <span class="s2">"</span><span class="s">/my-blog"</span>

<span class="c1"># 저장소 이름이 username.github.io라면</span>
<span class="na">baseurl</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
</code></pre></div></div>

<hr />

<h3 id="잔디-그래프가-비어-있을-때">잔디 그래프가 비어 있을 때</h3>

<p>GitHub 토큰이 없거나 <code class="language-plaintext highlighter-rouge">_data/theme.yml</code>의 <code class="language-plaintext highlighter-rouge">username</code>이 비어 있을 때 발생합니다.<br />
GitHub 연동을 아직 하지 않은 경우라면 <code class="language-plaintext highlighter-rouge">hero.github_contributions.enabled: false</code>로 잠시 꺼두면 됩니다.</p>

<hr />

<h3 id="jekyll-serve-후-파일을-수정해도-반영이-안-될-때"><code class="language-plaintext highlighter-rouge">jekyll serve</code> 후 파일을 수정해도 반영이 안 될 때</h3>

<p><code class="language-plaintext highlighter-rouge">_config.yml</code>을 수정했다면 서버를 재시작해야 합니다. 다른 파일은 저장과 동시에 자동으로 반영됩니다.</p>

<h2 id="추천-로컬-워크플로">추천 로컬 워크플로</h2>

<p>처음 테마를 가져간 뒤 아래 순서로 진행하면 가장 빠릅니다.</p>

<ol>
  <li><code class="language-plaintext highlighter-rouge">bundle install</code></li>
  <li><code class="language-plaintext highlighter-rouge">_config.yml</code>에서 <code class="language-plaintext highlighter-rouge">title</code>, <code class="language-plaintext highlighter-rouge">url</code>, <code class="language-plaintext highlighter-rouge">baseurl</code> 수정</li>
  <li><code class="language-plaintext highlighter-rouge">_data/profile.yml</code>에서 기본 이름과 소개 수정</li>
  <li><code class="language-plaintext highlighter-rouge">jekyll serve</code>로 로컬 미리보기</li>
  <li>만족스러우면 GitHub에 push → 자동 배포</li>
</ol>

<h2 id="다음-단계">다음 단계</h2>

<p>로컬 미리보기가 준비됐다면, 다음은 설정 파일을 내 블로그에 맞게 바꿀 차례입니다.</p>

<p>→ <strong><a href="/jekyll-theme-velog/customizing-profile-navigation-and-home/">프로필, 탭, 홈 화면을 테마 설정으로 커스터마이징하기</a></strong></p>]]></content><author><name></name></author><category term="Quick Start" /><category term="Local Preview" /><category term="Ruby" /><category term="Jekyll" /><summary type="html"><![CDATA[Ruby 의존성 설치부터 GitHub 연동 데이터 동기화, 로컬 미리보기와 흔한 오류 대응까지 한 번에 정리합니다.]]></summary></entry><entry><title type="html">프로필, 탭, 홈 화면을 테마 설정으로 커스터마이징하기</title><link href="https://woonyong-kr.github.io/jekyll-theme-velog/customizing-profile-navigation-and-home/" rel="alternate" type="text/html" title="프로필, 탭, 홈 화면을 테마 설정으로 커스터마이징하기" /><published>2026-03-31T09:00:00+09:00</published><updated>2026-03-31T09:00:00+09:00</updated><id>https://woonyong-kr.github.io/jekyll-theme-velog/customizing-profile-navigation-and-home</id><content type="html" xml:base="https://woonyong-kr.github.io/jekyll-theme-velog/customizing-profile-navigation-and-home/"><![CDATA[<p>이 테마는 HTML을 직접 열지 않아도 4개의 파일만 수정하면 대부분의 커스터마이징이 끝납니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>_config.yml           사이트 URL, 제목, 외부 연동
_data/theme.yml       UI 옵션, 탭, 헤더, 검색 문구
_data/profile.yml     기본 프로필 (GitHub 미연동 시 표시)
_data/series.yml      시리즈 제목과 설명
</code></pre></div></div>

<h2 id="_configyml--사이트-기본-정보"><code class="language-plaintext highlighter-rouge">_config.yml</code> — 사이트 기본 정보</h2>

<p>가장 먼저 수정해야 하는 파일입니다. 사이트 주소와 제목이 여기에 들어갑니다.</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">title</span><span class="pi">:</span> <span class="s">내 개발 블로그</span>
<span class="na">description</span><span class="pi">:</span> <span class="s">배운 것을 기록하는 공간</span>
<span class="na">url</span><span class="pi">:</span> <span class="s2">"</span><span class="s">https://username.github.io"</span>   <span class="c1"># 본인 GitHub 아이디로 변경</span>
<span class="na">baseurl</span><span class="pi">:</span> <span class="s2">"</span><span class="s">/my-blog"</span>                 <span class="c1"># 저장소 이름으로 변경</span>
</code></pre></div></div>

<blockquote>
  <p><strong><code class="language-plaintext highlighter-rouge">baseurl</code> 설정이 틀리면 CSS, 이미지, 링크가 모두 깨집니다.</strong><br />
저장소 이름이 <code class="language-plaintext highlighter-rouge">my-blog</code>라면 <code class="language-plaintext highlighter-rouge">baseurl: "/my-blog"</code>, <code class="language-plaintext highlighter-rouge">username.github.io</code>라면 <code class="language-plaintext highlighter-rouge">baseurl: ""</code>로 설정하세요.</p>
</blockquote>

<p>외부 서비스 연동도 여기서 합니다. 값이 비어 있으면 해당 기능은 자동으로 비활성화됩니다.</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">analytics</span><span class="pi">:</span>
  <span class="na">google</span><span class="pi">:</span>
    <span class="na">measurement_id</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>         <span class="c1"># Google Analytics ID (선택)</span>

<span class="na">comments</span><span class="pi">:</span>
  <span class="na">provider</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>                 <span class="c1"># "giscus" 또는 "disqus" (선택)</span>
  <span class="na">giscus</span><span class="pi">:</span>
    <span class="na">repo</span><span class="pi">:</span> <span class="s2">"</span><span class="s">owner/repository"</span>
    <span class="na">repo_id</span><span class="pi">:</span> <span class="s2">"</span><span class="s">..."</span>
    <span class="na">category</span><span class="pi">:</span> <span class="s2">"</span><span class="s">Announcements"</span>
    <span class="na">category_id</span><span class="pi">:</span> <span class="s2">"</span><span class="s">..."</span>

<span class="na">search</span><span class="pi">:</span>
  <span class="na">provider</span><span class="pi">:</span> <span class="s">local</span>              <span class="c1"># "local" 또는 "algolia"</span>
</code></pre></div></div>

<h2 id="_datathemeyml--ui-옵션과-문구"><code class="language-plaintext highlighter-rouge">_data/theme.yml</code> — UI 옵션과 문구</h2>

<p>화면에 보이는 거의 모든 옵션이 이 파일에 있습니다.</p>

<h3 id="헤더-버튼-노출-여부">헤더 버튼 노출 여부</h3>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">header</span><span class="pi">:</span>
  <span class="na">show_github_link</span><span class="pi">:</span> <span class="no">true</span>      <span class="c1"># 헤더 GitHub 링크 버튼</span>
  <span class="na">show_rss_link</span><span class="pi">:</span> <span class="no">true</span>         <span class="c1"># 헤더 RSS 버튼</span>
  <span class="na">show_theme_toggle</span><span class="pi">:</span> <span class="no">true</span>     <span class="c1"># 라이트/다크 토글 버튼</span>
</code></pre></div></div>

<h3 id="탭-구성">탭 구성</h3>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">tabs</span><span class="pi">:</span>
  <span class="na">posts_label</span><span class="pi">:</span> <span class="s">글</span>             <span class="c1"># "글" 탭 표시 이름</span>
  <span class="na">series_label</span><span class="pi">:</span> <span class="s">시리즈</span>        <span class="c1"># "시리즈" 탭 표시 이름</span>
  <span class="na">about_label</span><span class="pi">:</span> <span class="s">소개</span>           <span class="c1"># "소개" 탭 표시 이름</span>
  <span class="na">show_about</span><span class="pi">:</span> <span class="no">false</span>           <span class="c1"># 소개 탭 노출 여부 (true/false)</span>
</code></pre></div></div>

<p>소개 페이지가 준비되지 않았다면 <code class="language-plaintext highlighter-rouge">show_about: false</code>로 숨겨두세요.</p>

<h3 id="홈-화면-옵션">홈 화면 옵션</h3>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">home</span><span class="pi">:</span>
  <span class="na">initial_post_count</span><span class="pi">:</span> <span class="m">12</span>          <span class="c1"># 처음 로딩할 글 수</span>
  <span class="na">search_placeholder</span><span class="pi">:</span> <span class="s">검색어를 입력하세요</span>
  <span class="na">all_posts_label</span><span class="pi">:</span> <span class="s">전체보기</span>
  <span class="na">more_posts_label</span><span class="pi">:</span> <span class="s">스크롤하면 더 많은 글을 볼 수 있습니다.</span>
  <span class="na">empty_posts_label</span><span class="pi">:</span> <span class="s">조건에 맞는 글이 없습니다.</span>
</code></pre></div></div>

<h3 id="github-프로필-연동">GitHub 프로필 연동</h3>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">hero</span><span class="pi">:</span>
  <span class="na">show_stats</span><span class="pi">:</span> <span class="no">true</span>              <span class="c1"># 글 수, 시리즈 수 통계 표시</span>
  <span class="na">github_contributions</span><span class="pi">:</span>
    <span class="na">enabled</span><span class="pi">:</span> <span class="no">false</span>              <span class="c1"># 기여 그래프 사용 여부</span>
    <span class="na">username</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>                <span class="c1"># 본인 GitHub 아이디</span>

<span class="na">profile</span><span class="pi">:</span>
  <span class="na">github_sync</span><span class="pi">:</span>
    <span class="na">enabled</span><span class="pi">:</span> <span class="no">false</span>              <span class="c1"># GitHub 프로필 자동 동기화</span>
</code></pre></div></div>

<p>GitHub 연동 설정 방법은 <a href="/jekyll-theme-velog/github-profile-sync-and-contribution-graph/">GitHub 프로필 동기화 글</a>에서 상세히 설명합니다.</p>

<h3 id="푸터-문구">푸터 문구</h3>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">footer</span><span class="pi">:</span>
  <span class="na">tagline</span><span class="pi">:</span> <span class="s">Open-source Jekyll theme for developer blogs.</span>
</code></pre></div></div>

<h2 id="_dataprofileyml--기본-프로필"><code class="language-plaintext highlighter-rouge">_data/profile.yml</code> — 기본 프로필</h2>

<p>GitHub 프로필 동기화를 사용하지 않거나 동기화가 실패했을 때 보여줄 기본값입니다.</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">display_name</span><span class="pi">:</span> <span class="s">홍길동</span>
<span class="na">bio</span><span class="pi">:</span> <span class="s">백엔드 개발자, 배운 것을 기록합니다</span>
<span class="na">intro</span><span class="pi">:</span> <span class="s">현재 Python과 Django를 주로 사용합니다</span>
<span class="na">avatar</span><span class="pi">:</span> <span class="s">/assets/images/avatar-placeholder.svg</span>
<span class="na">github</span><span class="pi">:</span> <span class="s">https://github.com/your-handle</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">avatar</code>에는 로컬 이미지 경로 또는 외부 이미지 URL을 넣을 수 있습니다.</p>

<h2 id="_dataseriesyml--시리즈-정의"><code class="language-plaintext highlighter-rouge">_data/series.yml</code> — 시리즈 정의</h2>

<p>시리즈 키와 표시용 제목, 설명을 분리하는 파일입니다.</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">my-dev-log</span><span class="pi">:</span>
  <span class="na">title</span><span class="pi">:</span> <span class="s">개발 일지</span>
  <span class="na">description</span><span class="pi">:</span> <span class="s">매일 배운 것을 짧게 기록합니다</span>

<span class="na">backend-study</span><span class="pi">:</span>
  <span class="na">title</span><span class="pi">:</span> <span class="s">백엔드 공부</span>
  <span class="na">description</span><span class="pi">:</span> <span class="s">Django, Spring Boot, PostgreSQL 학습 기록</span>
</code></pre></div></div>

<p>포스트에서는 키만 사용합니다.</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">series</span><span class="pi">:</span> <span class="s">my-dev-log</span>
<span class="nn">---</span>
</code></pre></div></div>

<p>시리즈 제목이나 설명을 바꾸고 싶으면 각 포스트를 하나씩 수정할 필요 없이 <code class="language-plaintext highlighter-rouge">series.yml</code> 한 곳만 바꾸면 됩니다.</p>

<h2 id="빠른-브랜드-교체-체크리스트">빠른 브랜드 교체 체크리스트</h2>

<p>fork 직후 아래 순서로 빠르게 바꾸는 게 가장 효율적입니다.</p>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">_config.yml</code> — <code class="language-plaintext highlighter-rouge">title</code>, <code class="language-plaintext highlighter-rouge">description</code>, <code class="language-plaintext highlighter-rouge">url</code>, <code class="language-plaintext highlighter-rouge">baseurl</code></li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">_data/profile.yml</code> — 이름, 소개, GitHub 링크</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">_data/theme.yml</code> — 헤더 버튼, 탭 노출 여부, <code class="language-plaintext highlighter-rouge">footer.tagline</code></li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">_data/series.yml</code> — 샘플 시리즈 제거 후 본인 시리즈 추가</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">_posts/</code> — 샘플 글 유지 또는 교체</li>
</ul>

<h2 id="htmlcss는-언제-열어야-하나요">HTML/CSS는 언제 열어야 하나요?</h2>

<p>대부분은 데이터 파일 수정만으로 충분하지만, 아래 경우에는 템플릿이나 CSS를 직접 수정해야 합니다.</p>

<ul>
  <li>카드 간격이나 폰트 크기를 바꾸고 싶을 때 → <code class="language-plaintext highlighter-rouge">assets/css/</code></li>
  <li>레이아웃 구조를 바꾸고 싶을 때 → <code class="language-plaintext highlighter-rouge">_layouts/</code>, <code class="language-plaintext highlighter-rouge">_includes/</code></li>
  <li>헤더 아이콘 모양을 교체하고 싶을 때 → <code class="language-plaintext highlighter-rouge">_includes/</code></li>
</ul>

<p><strong>기준:</strong> 텍스트와 옵션은 데이터 파일, 구조와 스타일은 레이아웃/CSS.</p>

<h2 id="다음-단계">다음 단계</h2>

<p>설정을 완료했다면 이제 첫 번째 글을 작성할 차례입니다.</p>

<p>→ <strong><a href="/jekyll-theme-velog/writing-posts-series-and-thumbnails/">글, 시리즈, 태그, 썸네일을 이 테마에서 다루는 방법</a></strong></p>]]></content><author><name></name></author><category term="Theme Config" /><category term="Navigation" /><category term="Profile" /><category term="Customization" /><summary type="html"><![CDATA[브랜드 문구와 탭 노출 여부, 헤더 버튼, 홈 검색 문구, GitHub 동기화 옵션을 어디서 조절하는지 체계적으로 정리합니다.]]></summary></entry><entry><title type="html">글, 시리즈, 태그, 썸네일을 이 테마에서 다루는 방법</title><link href="https://woonyong-kr.github.io/jekyll-theme-velog/writing-posts-series-and-thumbnails/" rel="alternate" type="text/html" title="글, 시리즈, 태그, 썸네일을 이 테마에서 다루는 방법" /><published>2026-03-30T09:00:00+09:00</published><updated>2026-03-30T09:00:00+09:00</updated><id>https://woonyong-kr.github.io/jekyll-theme-velog/writing-posts-series-and-thumbnails</id><content type="html" xml:base="https://woonyong-kr.github.io/jekyll-theme-velog/writing-posts-series-and-thumbnails/"><![CDATA[<p>이 테마에서 글을 작성하는 방법은 단순합니다. <code class="language-plaintext highlighter-rouge">_posts/</code> 폴더에 마크다운 파일을 만들면 끝입니다. 다만 파일명 규칙과 front matter 몇 가지를 지키면 글 목록, 시리즈, 태그가 자동으로 연결됩니다.</p>

<h2 id="파일-만들기">파일 만들기</h2>

<h3 id="파일명-규칙">파일명 규칙</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>YYYY-MM-DD-영문-슬러그.md
</code></pre></div></div>

<p>예시:</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>2026-04-09-my-first-post.md
2026-04-10-django-rest-framework-tutorial.md
</code></pre></div></div>

<blockquote>
  <p><strong>주의:</strong> <code class="language-plaintext highlighter-rouge">date</code>가 오늘 이후 날짜이면 빌드 시 글이 보이지 않을 수 있습니다.<br />
반드시 현재 날짜 이전으로 설정하세요.</p>
</blockquote>

<h3 id="저장-위치">저장 위치</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>jekyll-theme-velog/
└── _posts/
    ├── 2026-04-09-my-first-post.md   ← 여기에 저장
    └── ...
</code></pre></div></div>

<p>GitHub 웹에서 바로 작성하려면:</p>

<ol>
  <li>저장소에서 <code class="language-plaintext highlighter-rouge">_posts</code> 폴더 클릭</li>
  <li><strong>Add file → Create new file</strong> 클릭</li>
  <li>파일명 입력 후 내용 작성</li>
  <li><strong>Commit changes</strong> 클릭</li>
</ol>

<h2 id="front-matter-전체-예시">front matter 전체 예시</h2>

<div class="language-md highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">title</span><span class="pi">:</span> <span class="s">글 제목을 여기에 씁니다</span>
<span class="na">description</span><span class="pi">:</span> <span class="s">글 목록 카드에 보일 한 줄 요약입니다</span>
<span class="na">date</span><span class="pi">:</span> <span class="s">2026-04-09 09:00:00 +0900</span>
<span class="na">updated_at</span><span class="pi">:</span> <span class="s">2026-04-09 21:00:00 +0900</span>
<span class="na">thumbnail</span><span class="pi">:</span> <span class="s">/assets/images/posts/my-cover.png</span>
<span class="na">series</span><span class="pi">:</span> <span class="s">my-series</span>
<span class="na">tags</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">Python</span>
  <span class="pi">-</span> <span class="s">Django</span>
  <span class="pi">-</span> <span class="s">Tutorial</span>
<span class="nn">---</span>

여기서부터 본문을 마크다운으로 씁니다.
</code></pre></div></div>

<h2 id="각-필드-설명">각 필드 설명</h2>

<table>
  <thead>
    <tr>
      <th>필드</th>
      <th>필수</th>
      <th>화면에서 쓰이는 곳</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">title</code></td>
      <td>✅</td>
      <td>글 목록 제목, 상세 제목, 브라우저 탭</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">description</code></td>
      <td>권장</td>
      <td>글 목록 카드 요약 문구</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">date</code></td>
      <td>✅</td>
      <td>날짜 정렬, 발행일 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">updated_at</code></td>
      <td>선택</td>
      <td>수정일 표시</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">thumbnail</code></td>
      <td>선택</td>
      <td>글 카드 이미지, 상세 상단 이미지</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">series</code></td>
      <td>선택</td>
      <td>시리즈 카드 그룹화, 시리즈 필터</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">tags</code></td>
      <td>선택</td>
      <td>홈 태그 패널, 글 상세 태그 링크</td>
    </tr>
  </tbody>
</table>

<h2 id="시리즈-연결하기">시리즈 연결하기</h2>

<h3 id="1-_dataseriesyml에-시리즈-등록">1. <code class="language-plaintext highlighter-rouge">_data/series.yml</code>에 시리즈 등록</h3>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">django-study</span><span class="pi">:</span>
  <span class="na">title</span><span class="pi">:</span> <span class="s">Django 공부 기록</span>
  <span class="na">description</span><span class="pi">:</span> <span class="s">Django 기초부터 배포까지 학습하면서 기록합니다</span>
</code></pre></div></div>

<h3 id="2-포스트-front-matter에-키-입력">2. 포스트 front matter에 키 입력</h3>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">series</span><span class="pi">:</span> <span class="s">django-study</span>
</code></pre></div></div>

<p>이렇게 하면 같은 <code class="language-plaintext highlighter-rouge">series</code> 키를 가진 글들이 시리즈 탭에 카드로 묶입니다. 시리즈 카드에는 <code class="language-plaintext highlighter-rouge">series.yml</code>에서 가져온 제목과 설명이 표시됩니다.</p>

<p><strong>장점:</strong> 시리즈 이름을 바꾸고 싶으면 <code class="language-plaintext highlighter-rouge">series.yml</code> 한 곳만 수정하면 됩니다. 각 포스트를 하나씩 바꿀 필요가 없습니다.</p>

<h2 id="태그-사용-팁">태그 사용 팁</h2>

<p>태그는 두 군데에서 동시에 쓰입니다.</p>

<ul>
  <li>홈 좌측 <strong>태그 패널</strong> — 클릭하면 해당 태그의 글만 필터링</li>
  <li><strong>글 카드와 상세</strong> 하단 태그 링크</li>
</ul>

<p>태그는 탐색 체계입니다. 너무 세분화하면 목록이 지저분해지고, 너무 넓으면 필터 효과가 없습니다. 글 5~10개당 태그 10개 이하를 권장합니다.</p>

<h2 id="썸네일-규칙">썸네일 규칙</h2>

<p>썸네일은 <code class="language-plaintext highlighter-rouge">thumbnail</code> 필드가 있을 때만 표시됩니다. 없으면 텍스트 중심 카드로 자연스럽게 표시됩니다.</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 로컬 이미지 사용</span>
<span class="na">thumbnail</span><span class="pi">:</span> <span class="s">/assets/images/posts/my-cover.png</span>

<span class="c1"># 외부 이미지 사용</span>
<span class="na">thumbnail</span><span class="pi">:</span> <span class="s">https://example.com/image.png</span>
</code></pre></div></div>

<p>이미지 파일은 <code class="language-plaintext highlighter-rouge">assets/images/posts/</code> 폴더에 넣으면 됩니다.</p>

<p><strong>권장 이미지 비율:</strong> 가로형(16:9 또는 2:1)을 쓰면 카드와 상세 페이지 모두에서 자연스럽게 보입니다.</p>

<h2 id="markdown-지원-범위">Markdown 지원 범위</h2>

<p>이 테마에서 지원이 확인된 Markdown 요소입니다.</p>

<div class="language-md highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># 제목 1</span>
<span class="gu">## 제목 2</span>
<span class="gu">### 제목 3</span>

일반 문단 텍스트

<span class="gs">**굵게**</span>, _기울여_, ~~취소선~~
<span class="p">
-</span> 목록 항목
<span class="p">  -</span> 중첩 목록
<span class="p">
1.</span> 번호 목록
<span class="p">
-</span> [x] 체크리스트 완료
<span class="p">-</span> [ ] 체크리스트 미완료

| 헤더1 | 헤더2 |
|---|---|
| 값1  | 값2  |
<span class="gt">
&gt; 인용문</span>

<span class="sb">`인라인 코드`</span>

<span class="p">```</span><span class="nl">python
</span><span class="c1"># 코드 블록 (언어 지정 가능)
</span><span class="k">def</span> <span class="nf">hello</span><span class="p">():</span>
    <span class="k">print</span><span class="p">(</span><span class="s">"Hello!"</span><span class="p">)</span>
</code></pre></div></div>

<p><img src="이미지 경로" alt="이미지 설명" /></p>

<p><a href="URL">링크 텍스트</a></p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
YouTube 영상은 iframe으로 직접 삽입할 수 있습니다.

```html
&lt;iframe width="560" height="315" src="https://www.youtube.com/embed/VIDEO_ID" frameborder="0" allowfullscreen&gt;&lt;/iframe&gt;
</code></pre></div></div>

<h2 id="글-발행-전-체크리스트">글 발행 전 체크리스트</h2>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">description</code>이 카드 요약으로 자연스러운지</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">date</code>가 현재 날짜 이전인지</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">series</code> 키가 <code class="language-plaintext highlighter-rouge">_data/series.yml</code>에 등록되어 있는지</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">thumbnail</code> 경로가 실제 파일과 일치하는지</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />태그가 너무 많지 않은지 (5개 이하 권장)</li>
</ul>

<h2 id="다음-단계">다음 단계</h2>

<p>글 작성 방법을 알았다면, 이제 배포할 차례입니다.</p>

<p>→ <strong><a href="/jekyll-theme-velog/deploying-with-github-pages-and-jenkins/">GitHub Pages와 Jenkins로 테마를 배포하는 방법</a></strong></p>]]></content><author><name></name></author><category term="Markdown" /><category term="Series" /><category term="Tags" /><category term="Thumbnail" /><summary type="html"><![CDATA[front matter 설계, Markdown 지원 범위, 시리즈와 태그 연결, 썸네일 표시 규칙까지 작성자 관점에서 정리합니다.]]></summary></entry><entry><title type="html">GitHub Pages와 Jenkins로 테마를 배포하는 방법</title><link href="https://woonyong-kr.github.io/jekyll-theme-velog/deploying-with-github-pages-and-jenkins/" rel="alternate" type="text/html" title="GitHub Pages와 Jenkins로 테마를 배포하는 방법" /><published>2026-03-29T09:00:00+09:00</published><updated>2026-03-29T09:00:00+09:00</updated><id>https://woonyong-kr.github.io/jekyll-theme-velog/deploying-with-github-pages-and-jenkins</id><content type="html" xml:base="https://woonyong-kr.github.io/jekyll-theme-velog/deploying-with-github-pages-and-jenkins/"><![CDATA[<p>이 테마는 <code class="language-plaintext highlighter-rouge">main</code> 브랜치에 push하면 GitHub Actions가 자동으로 빌드하고, 결과를 <code class="language-plaintext highlighter-rouge">gh-pages</code> 브랜치로 배포하는 구조입니다. 별도 서버 설정 없이 GitHub 저장소 설정 몇 가지만 바꾸면 됩니다.</p>

<h2 id="배포-흐름-한눈에-보기">배포 흐름 한눈에 보기</h2>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>내가 main에 push
    ↓
GitHub Actions 자동 실행
    ↓
1. gem 설치 (bundle install)
2. GitHub 프로필/기여 그래프 캐시 생성
3. jekyll build (_site/ 폴더 생성)
    ↓
_site/ 내용을 gh-pages 브랜치에 자동 push
    ↓
GitHub Pages가 gh-pages 브랜치를 서빙
    ↓
https://username.github.io/my-blog/ 접속 가능
</code></pre></div></div>

<h2 id="1단계--github-pages-설정하기">1단계 — GitHub Pages 설정하기</h2>

<p>이게 없으면 빌드가 성공해도 사이트가 열리지 않습니다.</p>

<ol>
  <li>fork한 저장소의 <strong>Settings</strong> 탭 클릭</li>
  <li>왼쪽 메뉴에서 <strong>Pages</strong> 클릭</li>
  <li><strong>Source</strong> 드롭다운에서 <code class="language-plaintext highlighter-rouge">Deploy from a branch</code> 선택</li>
  <li><strong>Branch</strong> 드롭다운에서 <code class="language-plaintext highlighter-rouge">gh-pages</code> 선택, 폴더는 <code class="language-plaintext highlighter-rouge">/ (root)</code> 선택</li>
  <li><strong>Save</strong> 클릭</li>
</ol>

<blockquote>
  <p><strong><code class="language-plaintext highlighter-rouge">gh-pages</code> 브랜치가 목록에 없을 때:</strong><br />
<code class="language-plaintext highlighter-rouge">main</code>에 push를 한 번 하면 GitHub Actions가 자동으로 <code class="language-plaintext highlighter-rouge">gh-pages</code> 브랜치를 만듭니다.<br />
저장소의 <strong>Actions</strong> 탭에서 빌드 완료 후 다시 시도해보세요.</p>
</blockquote>

<h2 id="2단계--_configyml-url-설정-확인">2단계 — <code class="language-plaintext highlighter-rouge">_config.yml</code> URL 설정 확인</h2>

<p>배포 주소와 <code class="language-plaintext highlighter-rouge">_config.yml</code> 설정이 반드시 일치해야 합니다.</p>

<h3 id="경우-1-프로젝트-페이지-가장-일반적">경우 1: 프로젝트 페이지 (가장 일반적)</h3>

<p>저장소 이름이 <code class="language-plaintext highlighter-rouge">my-blog</code>이고, GitHub 아이디가 <code class="language-plaintext highlighter-rouge">hong</code>이면:</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">url</span><span class="pi">:</span> <span class="s2">"</span><span class="s">https://hong.github.io"</span>
<span class="na">baseurl</span><span class="pi">:</span> <span class="s2">"</span><span class="s">/my-blog"</span>
</code></pre></div></div>

<p>접속 주소: <code class="language-plaintext highlighter-rouge">https://hong.github.io/my-blog/</code></p>

<h3 id="경우-2-사용자-페이지-저장소-이름이-usernamegithubio">경우 2: 사용자 페이지 (저장소 이름이 <code class="language-plaintext highlighter-rouge">username.github.io</code>)</h3>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">url</span><span class="pi">:</span> <span class="s2">"</span><span class="s">https://hong.github.io"</span>
<span class="na">baseurl</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
</code></pre></div></div>

<p>접속 주소: <code class="language-plaintext highlighter-rouge">https://hong.github.io/</code></p>

<h3 id="경우-3-커스텀-도메인-사용">경우 3: 커스텀 도메인 사용</h3>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">url</span><span class="pi">:</span> <span class="s2">"</span><span class="s">https://blog.mydomain.com"</span>
<span class="na">baseurl</span><span class="pi">:</span> <span class="s2">"</span><span class="s">"</span>
</code></pre></div></div>

<blockquote>
  <p><strong>가장 흔한 실수:</strong> <code class="language-plaintext highlighter-rouge">baseurl</code>을 잘못 설정하면 CSS와 이미지가 404가 납니다.<br />
배포 후 화면이 깨지면 가장 먼저 <code class="language-plaintext highlighter-rouge">baseurl</code>을 확인하세요.</p>
</blockquote>

<h2 id="3단계--배포-확인">3단계 — 배포 확인</h2>

<p>저장소 <strong>Actions</strong> 탭에서 빌드 진행 상황을 확인할 수 있습니다.</p>

<ul>
  <li>노란 원 🟡 = 빌드 진행 중</li>
  <li>초록 체크 ✅ = 빌드 성공, 사이트 열림</li>
  <li>빨간 X ❌ = 빌드 실패, 로그 확인 필요</li>
</ul>

<p>빌드 성공 후 1~2분 내에 사이트가 열립니다.</p>

<h2 id="커스텀-도메인-연결하기-선택">커스텀 도메인 연결하기 (선택)</h2>

<p>기본 <code class="language-plaintext highlighter-rouge">github.io</code> 주소 대신 본인 도메인을 연결할 수 있습니다.</p>

<h3 id="1-github-pages에-도메인-입력">1. GitHub Pages에 도메인 입력</h3>

<p><strong>Settings → Pages → Custom domain</strong>에 도메인 입력 후 Save.</p>

<h3 id="2-dns-설정">2. DNS 설정</h3>

<p>도메인 등록 업체(가비아, Cloudflare 등)에서 DNS 레코드를 추가합니다.</p>

<p><strong>루트 도메인</strong> (<code class="language-plaintext highlighter-rouge">example.com</code>):</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A 레코드 → 185.199.108.153
A 레코드 → 185.199.109.153
A 레코드 → 185.199.110.153
A 레코드 → 185.199.111.153
</code></pre></div></div>

<p><strong>서브도메인</strong> (<code class="language-plaintext highlighter-rouge">blog.example.com</code>):</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CNAME 레코드 → username.github.io
</code></pre></div></div>

<h3 id="3-https-활성화">3. HTTPS 활성화</h3>

<p>DNS 설정 후 GitHub Pages가 TLS 인증서를 발급합니다(보통 수 분~수십 분 소요).<br />
<strong>Settings → Pages</strong>에서 <code class="language-plaintext highlighter-rouge">Enforce HTTPS</code> 버튼이 활성화되면 클릭해서 켜세요.</p>

<blockquote>
  <p>인증서가 오래 걸릴 때는 Custom domain을 한 번 제거 후 다시 입력하면 재시도됩니다.</p>
</blockquote>

<h2 id="자동-배포가-안-될-때-확인할-것">자동 배포가 안 될 때 확인할 것</h2>

<h3 id="github-actions-권한-오류">GitHub Actions 권한 오류</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Error: Permission denied to github-actions[bot]
</code></pre></div></div>

<p><strong>Settings → Actions → General → Workflow permissions</strong>에서<br />
<code class="language-plaintext highlighter-rouge">Read and write permissions</code>를 선택하고 저장하세요.</p>

<hr />

<h3 id="빌드는-성공했는데-사이트가-안-열릴-때">빌드는 성공했는데 사이트가 안 열릴 때</h3>

<ol>
  <li><strong>Settings → Pages</strong>에서 source가 <code class="language-plaintext highlighter-rouge">gh-pages / (root)</code>로 설정됐는지 확인</li>
  <li><code class="language-plaintext highlighter-rouge">gh-pages</code> 브랜치에 실제 파일이 있는지 확인</li>
  <li><code class="language-plaintext highlighter-rouge">_config.yml</code>의 <code class="language-plaintext highlighter-rouge">url</code>과 <code class="language-plaintext highlighter-rouge">baseurl</code>이 올바른지 확인</li>
</ol>

<hr />

<h3 id="빌드-실패-시">빌드 실패 시</h3>

<p>Actions 탭에서 실패한 job을 클릭하면 상세 로그를 볼 수 있습니다. 가장 흔한 원인은 <code class="language-plaintext highlighter-rouge">Gemfile.lock</code> 충돌 또는 잘못된 YAML 문법입니다.</p>

<h2 id="jenkins로-배포하기">Jenkins로 배포하기</h2>

<p>GitHub Actions 대신 Jenkins를 쓰는 환경이라면 저장소에 포함된 <code class="language-plaintext highlighter-rouge">Jenkinsfile</code>을 그대로 사용할 수 있습니다.</p>

<h3 id="필요한-credentials">필요한 Credentials</h3>

<p>Jenkins에 아래 자격증명을 등록합니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Kind: Username with password
ID: github-pages
Username: GitHub 사용자명
Password: GitHub Personal Access Token (repo 권한)
</code></pre></div></div>

<h3 id="jenkins-에이전트-요구사항">Jenkins 에이전트 요구사항</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>- Ruby 3.2.x
- Bundler
- Git
</code></pre></div></div>

<h3 id="배포-흐름">배포 흐름</h3>

<p>Jenkinsfile의 파이프라인은 GitHub Actions와 동일한 흐름입니다.</p>

<ol>
  <li>gem 설치</li>
  <li>GitHub 프로필/기여 그래프 캐시 갱신</li>
  <li><code class="language-plaintext highlighter-rouge">jekyll build</code></li>
  <li><code class="language-plaintext highlighter-rouge">gh-pages</code> 브랜치에 push</li>
</ol>

<h2 id="배포-후-체크리스트">배포 후 체크리스트</h2>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />사이트 주소로 접속 시 200 응답 확인</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />홈, 시리즈, 글 상세 페이지 정상 표시 확인</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />CSS와 이미지가 깨지지 않는지 확인</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">feed.xml</code> 접속 확인 (<code class="language-plaintext highlighter-rouge">/feed.xml</code>)</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />커스텀 도메인 사용 시 HTTPS 작동 확인</li>
</ul>

<h2 id="다음-단계">다음 단계</h2>

<p>배포가 완료됐다면 GitHub 프로필과 기여 그래프를 블로그에 연결해보세요.</p>

<p>→ <strong><a href="/jekyll-theme-velog/github-profile-sync-and-contribution-graph/">GitHub 프로필 동기화와 기여 그래프를 붙이는 방법</a></strong></p>]]></content><author><name></name></author><category term="GitHub Pages" /><category term="Jenkins" /><category term="Deployment" /><category term="Open Source" /><summary type="html"><![CDATA[프로젝트 페이지 기준으로 빌드 결과를 gh-pages 브랜치에 배포하는 흐름과 저장소 이름 변경 시 주의할 점까지 정리합니다.]]></summary></entry><entry><title type="html">GitHub 프로필 동기화와 기여 그래프를 붙이는 방법</title><link href="https://woonyong-kr.github.io/jekyll-theme-velog/github-profile-sync-and-contribution-graph/" rel="alternate" type="text/html" title="GitHub 프로필 동기화와 기여 그래프를 붙이는 방법" /><published>2026-03-28T09:00:00+09:00</published><updated>2026-03-28T09:00:00+09:00</updated><id>https://woonyong-kr.github.io/jekyll-theme-velog/github-profile-sync-and-contribution-graph</id><content type="html" xml:base="https://woonyong-kr.github.io/jekyll-theme-velog/github-profile-sync-and-contribution-graph/"><![CDATA[<p>이 테마는 GitHub 계정을 연결하면 홈의 프로필 이름, 아바타, 기여 그래프를 자동으로 채웁니다. 연결이 실패하거나 설정을 안 해도 화면이 깨지지 않는 fallback 구조도 함께 갖고 있습니다.</p>

<h2 id="어떤-데이터를-가져오나요">어떤 데이터를 가져오나요?</h2>

<p>동기화 스크립트는 두 종류의 캐시 파일을 만듭니다.</p>

<table>
  <thead>
    <tr>
      <th>파일</th>
      <th>내용</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">_data/github_profile_cache.json</code></td>
      <td>이름, 아바타 URL, GitHub 링크, 소개</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">_data/github_contributions_cache.json</code></td>
      <td>최근 1년간 날짜별 기여 횟수</td>
    </tr>
  </tbody>
</table>

<p>빌드 시에는 이 캐시 파일만 읽기 때문에 <strong>브라우저에 토큰이 노출되지 않습니다.</strong></p>

<h2 id="설정-방법">설정 방법</h2>

<h3 id="1-_datathemeyml-수정">1. <code class="language-plaintext highlighter-rouge">_data/theme.yml</code> 수정</h3>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">hero</span><span class="pi">:</span>
  <span class="na">github_contributions</span><span class="pi">:</span>
    <span class="na">enabled</span><span class="pi">:</span> <span class="no">true</span>          <span class="c1"># 기여 그래프 활성화</span>
    <span class="na">username</span><span class="pi">:</span> <span class="s">your-id</span>      <span class="c1"># 본인 GitHub 아이디 (예: hong)</span>

<span class="na">profile</span><span class="pi">:</span>
  <span class="na">github_sync</span><span class="pi">:</span>
    <span class="na">enabled</span><span class="pi">:</span> <span class="no">true</span>          <span class="c1"># 프로필 동기화 활성화</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">username</code>이 비어 있으면 <code class="language-plaintext highlighter-rouge">_data/profile.yml</code>의 <code class="language-plaintext highlighter-rouge">github</code> 링크에서 아이디를 유추합니다.</p>

<h3 id="2-인증-준비">2. 인증 준비</h3>

<p>아래 세 가지 중 하나면 됩니다.</p>

<p><strong>방법 A — GitHub CLI (가장 쉬움)</strong></p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>gh auth login
</code></pre></div></div>

<p>프롬프트에 따라 브라우저에서 GitHub 로그인하면 자동으로 인증됩니다.</p>

<p><strong>방법 B — Personal Access Token</strong></p>

<ol>
  <li>GitHub → <strong>Settings → Developer settings → Personal access tokens → Fine-grained tokens</strong></li>
  <li><strong>Generate new token</strong> 클릭</li>
  <li>Repository access: <code class="language-plaintext highlighter-rouge">Public Repositories (read-only)</code></li>
  <li>Permissions에서 <code class="language-plaintext highlighter-rouge">read:user</code> 선택</li>
  <li>토큰 생성 후 복사</li>
</ol>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">GITHUB_TOKEN</span><span class="o">=</span>ghp_xxxxxxxxxxxx
</code></pre></div></div>

<blockquote>
  <p>토큰은 한 번만 보여줍니다. 반드시 복사해두세요.</p>
</blockquote>

<h3 id="3-동기화-스크립트-실행">3. 동기화 스크립트 실행</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ruby scripts/fetch_github_contributions.rb
</code></pre></div></div>

<p>성공하면 아래처럼 출력됩니다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Fetching GitHub profile for: your-id
✓ Profile saved to _data/github_profile_cache.json
✓ Contributions saved to _data/github_contributions_cache.json
</code></pre></div></div>

<h3 id="4-결과-확인">4. 결과 확인</h3>

<p>로컬에서 확인:</p>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">BUNDLE_FORCE_RUBY_PLATFORM</span><span class="o">=</span><span class="nb">true </span>bundle <span class="nb">exec </span>jekyll serve
</code></pre></div></div>

<p>홈 화면에 본인 GitHub 아바타, 이름, 기여 그래프가 표시되면 성공입니다.</p>

<h2 id="배포-환경에서-자동-동기화">배포 환경에서 자동 동기화</h2>

<p>로컬에서 수동으로 실행하는 방법 외에, GitHub Actions로 배포할 때 자동으로 동기화됩니다.</p>

<h3 id="1-repository-secret-등록">1. Repository Secret 등록</h3>

<p>저장소 <strong>Settings → Secrets and variables → Actions → New repository secret</strong></p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Name: GH_PAT
Secret: (위에서 만든 Personal Access Token 값)
</code></pre></div></div>

<h3 id="2-자동-동기화-스케줄-확인">2. 자동 동기화 스케줄 확인</h3>

<p>이 저장소의 <code class="language-plaintext highlighter-rouge">.github/workflows/deploy.yml</code>에는 두 가지 트리거가 있습니다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">main</code> 브랜치 push 시 → 배포와 함께 프로필 동기화</li>
  <li><strong>매일 새벽 3시 17분</strong> → 기여 그래프 자동 갱신</li>
</ul>

<p>즉, 새 글을 push할 때마다 기여 그래프도 최신으로 업데이트됩니다.</p>

<h2 id="fallback-규칙">fallback 규칙</h2>

<p>연결이 완벽하지 않아도 화면이 망가지지 않습니다.</p>

<table>
  <thead>
    <tr>
      <th>상황</th>
      <th>화면 동작</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>GitHub 아바타 있음</td>
      <td>GitHub 아바타 사용</td>
    </tr>
    <tr>
      <td>GitHub 아바타 없음</td>
      <td><code class="language-plaintext highlighter-rouge">_data/profile.yml</code>의 <code class="language-plaintext highlighter-rouge">avatar</code> 사용</td>
    </tr>
    <tr>
      <td>GitHub 이름 있음</td>
      <td>GitHub 이름으로 표시</td>
    </tr>
    <tr>
      <td>GitHub <code class="language-plaintext highlighter-rouge">bio</code> 비어 있음</td>
      <td><code class="language-plaintext highlighter-rouge">_data/profile.yml</code>의 소개 유지</td>
    </tr>
    <tr>
      <td>기여 그래프 캐시 없음</td>
      <td>마지막 캐시 또는 빈 그래프 표시</td>
    </tr>
  </tbody>
</table>

<p>즉, 처음 fork했을 때는 <code class="language-plaintext highlighter-rouge">_data/profile.yml</code>의 샘플 데이터가 보이고, GitHub 연동을 완료하면 자동으로 실제 데이터로 교체됩니다.</p>

<h2 id="동기화가-안-될-때-확인할-것">동기화가 안 될 때 확인할 것</h2>

<h3 id="스크립트-실행-후-파일이-생성됐는지-확인">스크립트 실행 후 파일이 생성됐는지 확인</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">ls </span>_data/github_<span class="k">*</span>_cache.json
</code></pre></div></div>

<p>파일이 없다면 인증이 실패한 것입니다.</p>

<hr />

<h3 id="인증-오류가-날-때">인증 오류가 날 때</h3>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Error: Could not authenticate with GitHub API
</code></pre></div></div>

<ul>
  <li><code class="language-plaintext highlighter-rouge">GITHUB_TOKEN</code> 환경 변수가 설정됐는지 확인</li>
  <li><code class="language-plaintext highlighter-rouge">gh auth status</code>로 GitHub CLI 로그인 상태 확인</li>
  <li>토큰이 만료됐거나 권한이 부족하지 않은지 확인</li>
</ul>

<hr />

<h3 id="프로필은-동기화됐는데-기여-그래프가-안-보일-때">프로필은 동기화됐는데 기여 그래프가 안 보일 때</h3>

<p><code class="language-plaintext highlighter-rouge">_data/theme.yml</code>에서 <code class="language-plaintext highlighter-rouge">hero.github_contributions.enabled</code>가 <code class="language-plaintext highlighter-rouge">true</code>인지,<br />
<code class="language-plaintext highlighter-rouge">username</code>이 실제 GitHub 아이디와 일치하는지 확인하세요.</p>

<hr />

<h3 id="github-actions에서-동기화-실패할-때">GitHub Actions에서 동기화 실패할 때</h3>

<p>저장소 <strong>Settings → Secrets and variables → Actions</strong>에서 <code class="language-plaintext highlighter-rouge">GH_PAT</code> 시크릿이 등록됐는지 확인하세요. 없으면 Actions가 GitHub API에 접근할 수 없습니다.</p>

<h2 id="github-연동-없이-프로필만-꾸미기">GitHub 연동 없이 프로필만 꾸미기</h2>

<p>GitHub 동기화를 쓰지 않더라도 <code class="language-plaintext highlighter-rouge">_data/profile.yml</code>을 직접 편집하면 됩니다.</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">display_name</span><span class="pi">:</span> <span class="s">홍길동</span>
<span class="na">bio</span><span class="pi">:</span> <span class="s">백엔드 개발자, 기록을 좋아합니다</span>
<span class="na">intro</span><span class="pi">:</span> <span class="s">Python, Django, PostgreSQL 주로 사용</span>
<span class="na">avatar</span><span class="pi">:</span> <span class="s">/assets/images/my-avatar.jpg</span>   <span class="c1"># 직접 이미지 업로드</span>
<span class="na">github</span><span class="pi">:</span> <span class="s">https://github.com/your-handle</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">avatar</code>에 외부 URL(예: GitHub 프로필 이미지 URL)을 직접 넣어도 됩니다.</p>

<div class="language-yml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">avatar</span><span class="pi">:</span> <span class="s">https://avatars.githubusercontent.com/u/12345678</span>
</code></pre></div></div>

<h2 id="정리">정리</h2>

<p>이 테마의 GitHub 연동 구조를 한 줄로 정리하면:</p>

<blockquote>
  <p><strong>연결되면 자동으로 좋아지고, 연결이 안 돼도 깨지지 않는다.</strong></p>
</blockquote>

<p>처음에는 <code class="language-plaintext highlighter-rouge">_data/profile.yml</code>로 빠르게 채우고, 나중에 여유가 생기면 GitHub 동기화를 붙이는 방식으로도 충분합니다.</p>]]></content><author><name></name></author><category term="GitHub" /><category term="GraphQL" /><category term="Profile Sync" /><category term="Jekyll Theme" /><summary type="html"><![CDATA[GitHub 아이디만 연결하면 이름, 아바타, 링크, 기여 그래프가 홈 화면에 반영되는 흐름과 fallback 규칙을 설명합니다.]]></summary></entry></feed>